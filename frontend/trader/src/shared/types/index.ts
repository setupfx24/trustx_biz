/**
 * Shared type declarations.
 * Re-exports from the current types/ directory during migration.
 */

// Charting library types are referenced globally via .d.ts files
// and don't need re-export. This barrel is for future shared types.
