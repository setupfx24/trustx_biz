/**
 * Shared library utilities.
 * Import as: import { cn, formatPrice, formatMoney } from '@/shared/lib';
 */

export { cn, formatPrice, formatMoney, formatPercent, getDigits } from '@/lib/utils';
export { sounds, unlockAudio } from '@/lib/sounds';
